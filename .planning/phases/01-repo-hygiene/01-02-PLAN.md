---
phase: 01-repo-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - manifest.json
autonomous: true
requirements:
  - REPO-03
  - REPO-04

must_haves:
  truths:
    - "package.json has all required metadata fields for npm publishing"
    - "manifest.json paths all resolve to real files"
    - "npm pack --dry-run shows only intended plugin files"
  artifacts:
    - path: "package.json"
      provides: "Complete npm package metadata"
      contains: "repository"
    - path: "manifest.json"
      provides: "Valid Claude Code plugin manifest"
      contains: "agents"
  key_links:
    - from: "package.json"
      to: "npm registry"
      via: "npm publish reads metadata"
      pattern: "repository.*github"
    - from: "manifest.json"
      to: "agents/*.md"
      via: "file path references"
      pattern: "agents/.*\\.md"
---

<objective>
Complete package.json metadata for npm publishing and validate manifest.json file path references.

Purpose: package.json is missing repository, homepage, bugs, engines, bin, and files fields — meaning npm publish would leak all internal files and the npm page would have no GitHub link. manifest.json agent/skill paths must be verified to resolve to actual files so `claude plugin add` works without silent failures.

Output: Fully populated package.json and validated manifest.json. `npm pack --dry-run` shows only intended files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-repo-hygiene/01-CONTEXT.md

<interfaces>
<!-- Current package.json structure (to be extended, not replaced) -->
From package.json:
```json
{
  "name": "ui-ux-suite",
  "version": "0.1.0",
  "description": "Comprehensive UI/UX design intelligence suite for Claude Code...",
  "author": "Adam Boudjemaa",
  "license": "MIT",
  "main": "lib/mcp-server.js",
  "scripts": { "test": "node -e \"require('./lib/schema')...\"" },
  "keywords": ["ui", "ux", "design", "audit", "accessibility", "color", "typography", "claude-code-plugin"]
}
```

From manifest.json:
```json
{
  "name": "ui-ux-suite",
  "version": "0.1.0",
  "agents": ["agents/design-auditor.md", ...12 total],
  "skills": ["skills/design-audit.md", ...14 total],
  "mcpServer": "lib/mcp-server.js"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add complete metadata to package.json</name>
  <files>package.json</files>
  <read_first>package.json, .planning/phases/01-repo-hygiene/01-CONTEXT.md</read_first>
  <action>
Read the current package.json and add the missing fields per decisions D-04 through D-09. Keep ALL existing fields intact (name, version, description, author, license, main, scripts, keywords). Add these new fields:

Per D-08 — repository, homepage, bugs:
```json
"repository": {
  "type": "git",
  "url": "git+https://github.com/Aboudjem/ui-ux-suite.git"
},
"homepage": "https://github.com/Aboudjem/ui-ux-suite#readme",
"bugs": {
  "url": "https://github.com/Aboudjem/ui-ux-suite/issues"
},
```

Per D-07 — engines:
```json
"engines": {
  "node": ">=18"
},
```

Per D-05 — bin field for future npx support:
```json
"bin": {
  "ui-ux-suite": "./bin/cli.js"
},
```

Note: The bin/cli.js file does not need to exist yet — it will be created in a later phase. The field is declared now per D-05 for package.json completeness.

Per D-09 — files whitelist (this replaces .npmignore per Claude's discretion and pitfall research):
```json
"files": [
  "lib/",
  "agents/",
  "skills/",
  "knowledge/",
  "commands/",
  "templates/",
  "manifest.json",
  "LICENSE"
],
```

Per D-06 — extend keywords array to include all specified terms:
```json
"keywords": [
  "claude-code",
  "claude-code-plugin",
  "ui",
  "ux",
  "design",
  "audit",
  "accessibility",
  "color",
  "typography",
  "design-system",
  "design-tokens",
  "tailwind",
  "css",
  "wcag",
  "apca"
],
```

Write the complete package.json preserving the existing structure and adding these fields. Use 2-space indentation.

After writing, run `npm pack --dry-run` to verify only intended files appear in the package.
  </action>
  <verify>
    <automated>cd /Users/adamboudj/projects/ui-ux-suite && node -e "const p=require('./package.json'); const checks=['repository','homepage','bugs','engines','bin','files','keywords']; const missing=checks.filter(k=>!p[k]); if(missing.length){console.log('FAIL: missing',missing);process.exit(1)} if(p.engines.node!=='>=18'){console.log('FAIL: engines wrong');process.exit(1)} if(!p.repository.url.includes('Aboudjem/ui-ux-suite')){console.log('FAIL: repo url wrong');process.exit(1)} if(!p.keywords.includes('wcag')){console.log('FAIL: keywords incomplete');process.exit(1)} if(!p.files.includes('lib/')){console.log('FAIL: files missing lib/');process.exit(1)} console.log('PASS: package.json has all required fields')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "repository" field with "Aboudjem/ui-ux-suite" (grep-verifiable)
    - package.json contains "homepage" field with github URL (grep-verifiable)
    - package.json contains "bugs" field (grep-verifiable)
    - package.json contains "engines" with ">=18" (grep-verifiable)
    - package.json contains "bin" field (grep-verifiable)
    - package.json contains "files" array with "lib/", "agents/", "skills/", "knowledge/", "commands/", "templates/", "manifest.json", "LICENSE" (grep-verifiable)
    - package.json keywords include "wcag", "apca", "claude-code", "design-tokens", "tailwind" (grep-verifiable)
    - `npm pack --dry-run` does NOT list .deep-research/, .omc/, .planning/
  </acceptance_criteria>
  <done>package.json has repository, homepage, bugs, engines, bin, files, and expanded keywords. npm pack --dry-run shows only plugin files.</done>
</task>

<task type="auto">
  <name>Task 2: Validate manifest.json file references</name>
  <files>manifest.json</files>
  <read_first>manifest.json, .planning/phases/01-repo-hygiene/01-CONTEXT.md</read_first>
  <action>
Per D-12, keep root manifest.json as the plugin manifest. Per D-13, verify all agent/skill file paths resolve to actual files.

Read manifest.json and verify every path in the "agents" array (12 entries) and "skills" array (14 entries) resolves to a real file on disk. Also verify "mcpServer": "lib/mcp-server.js" resolves.

Run this verification script:
```bash
node -e "
const manifest = require('./manifest.json');
let missing = [];
const fs = require('fs');
[...manifest.agents, ...manifest.skills, manifest.mcpServer].forEach(p => {
  if (!fs.existsSync(p)) missing.push(p);
});
if (missing.length) {
  console.log('MISSING FILES:', missing);
  process.exit(1);
} else {
  console.log('All', manifest.agents.length, 'agents,', manifest.skills.length, 'skills, and mcpServer resolve OK');
}
"
```

If any paths are missing, fix the manifest to match actual file locations.

Additionally, verify the `commands/` directory files match the skills. The commands/ directory has 14 .md files that correspond to the 14 skills — this is correct and no manifest change is needed for commands (Claude Code discovers them by convention from the commands/ directory).

No changes needed to manifest.json if all paths resolve. If any path is broken, fix it.
  </action>
  <verify>
    <automated>cd /Users/adamboudj/projects/ui-ux-suite && node -e "const m=require('./manifest.json');const fs=require('fs');let ok=0,bad=[];[...m.agents,...m.skills,m.mcpServer].forEach(p=>{if(fs.existsSync(p))ok++;else bad.push(p)});if(bad.length){console.log('FAIL:',bad);process.exit(1)}console.log('PASS: all',ok,'manifest paths resolve')"</automated>
  </verify>
  <acceptance_criteria>
    - manifest.json exists with "agents" array of 12 entries (grep-verifiable: 12 occurrences of "agents/")
    - manifest.json exists with "skills" array of 14 entries (grep-verifiable: 14 occurrences of "skills/")
    - Every path in agents array resolves to an existing .md file
    - Every path in skills array resolves to an existing .md file
    - "mcpServer" value "lib/mcp-server.js" resolves to an existing file
    - All 26 component paths + 1 mcpServer path = 27 total verified
  </acceptance_criteria>
  <done>All 27 manifest.json file references (12 agents, 14 skills, 1 mcpServer) verified as resolving to actual files on disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm pack -> registry | files field controls what ships to npm |
| manifest.json -> Claude Code | broken paths cause silent plugin failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | npm package contents | mitigate | Use "files" whitelist in package.json (per D-09) instead of .npmignore; verify with npm pack --dry-run |
| T-01-05 | Denial of Service | Broken manifest paths | mitigate | Validate all 27 manifest file references resolve to real files |
| T-01-06 | Tampering | package.json bin field | accept | bin/cli.js does not exist yet; bin field is a forward declaration per D-05. No execution risk until file is created. |
</threat_model>

<verification>
1. package.json has repository, homepage, bugs, engines, bin, files, keywords fields
2. package.json "files" whitelist includes lib/, agents/, skills/, knowledge/, commands/, templates/, manifest.json, LICENSE
3. package.json keywords include all 15 terms from D-06
4. `npm pack --dry-run` shows only intended plugin files
5. All 12 agent paths in manifest.json resolve to files
6. All 14 skill paths in manifest.json resolve to files
7. mcpServer path resolves to lib/mcp-server.js
</verification>

<success_criteria>
- package.json passes metadata completeness check (all 7 new fields present)
- npm pack --dry-run excludes .deep-research/, .omc/, .planning/, .claude-plugin/
- All 27 manifest.json file references resolve to actual files
- No broken paths that would cause silent plugin installation failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-repo-hygiene/01-02-SUMMARY.md`
</output>
